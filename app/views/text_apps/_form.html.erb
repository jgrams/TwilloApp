<%= form_for(@text_app) do |f| %>
  <% if @text_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_app.errors.count, "error") %> prohibited this text_app from being saved:</h2>

      <ul>
      <% @text_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_num %><br>
    <%= f.text_field :phone_num %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
